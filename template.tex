\documentclass[12pt]{article}

\usepackage{geometry}

\usepackage[scaled]{uarial}
\renewcommand*\familydefault{\sfdefault} 
\usepackage[T1]{fontenc}

\geometry{
	a4paper,
	total={210mm,297mm},
	left=0mm,
	right=0mm,
	top=0mm,
	bottom=0mm,
}

\usepackage{tikz}
\usetikzlibrary{backgrounds,calc,positioning}

\usepackage{ wasysym }

\newcommand{\mcol}{white}
\newcommand{\mmcol}{yellow}
\newcommand{\lw}{50mm}
\newcommand{\tw}{\lw - 5mm}
\newcommand{\lh}{41mm}
\newcommand{\row}[1]{(\lh - 4mm) / 2 - (\lh / 18) - #1 * ((\lh - 4mm) / 9)}
\newcommand{\col}[2]{-\lw / 2 + \lw / (2 * #2) + #1 * (\lw / #2)}
\newcommand{\lbllmar}{-\lw / 2 - 2mm } 
\newcommand{\lblrmar}{\lw / 2 + 2mm } 

\newcommand{\musnum}{NMP6V}

\newcommand{\gender}[1]{
	\ifx#1m \male
	\else\if#1f \female
	\else #1
	\fi\fi
}

%Black Tex magic - macro with 10+ args
\newcommand{\mylabel}[2]{
	\def\tempgen{#1}%
	\def\tempspec{#2}%
	\mylabelcont
}
\newcommand{\mylabelcont}[8]{
	\noindent
	\begin{tikzpicture}[
		%framed,
		collection/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw}
		},
		species/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw}
		},
		who/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw}
		},
		sep/.style = {
			draw,
			line width=2.2pt
		},
		colId/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw / 2},
			text width={\tw},
			align=left
		},
		collectorId/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw / 4}
		},
		gender/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw / 4}
		},
		location/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw}
		},
		locationPrec/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw},
			text width={\tw},
			align=left
		},
		date/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw},
			text width={\tw},
			align=left
		},
		author/.style = {
			draw={\mcol},
			rectangle,
			minimum width={\lw},
			text width={\tw},
			align=left
		},
		counter/.style = {
			draw={\mcol},
			rectangle,
		},
		bb/.style = {
			draw={\mmcol},
			minimum width={\lw},
			minimum height={\lh},
		}
	]

	\node[collection]	at ( 0, {\row{0}}) {\fontsize{7pt}{1em}\selectfont Collectio Musei Nationalis Pragae};
	\node[species]		at ( 0, {\row{1}}) {\fontsize{12pt}{1em}\selectfont  \em \tempgen{} \tempspec};
	\node[who]		at ( 0, {\row{2}}) {\fontsize{7pt}{1em}\selectfont #6};
	\draw[sep] ({\lbllmar},{\row{3}}) -- ({\lblrmar},{\row{3}});
	\node[colId]		at ( 0, {\row{4}}) {\fontsize{8pt}{1em}\selectfont\musnum#3};
	\node[collectorId]	at ({\col{2}{4}}, {\row{4}}) {\fontsize{8pt}{1em}\selectfont(#8)};
	\node[gender]		at ({\col{3}{4}}, {\row{4}}) {\fontsize{8pt}{1em}\selectfont\gender{#5}};
	\node[location]		at ( 0, {\row{5}}) {\fontsize{12pt}{1em}\selectfont#7};
	\node[locationPrec]	at ( 0, {\row{6}}) {\fontsize{8pt}{1em}\selectfont#1};
	\node[date]		at ( 0, {\row{7}}) {\fontsize{8pt}{1em}\selectfont#2};
	\node[author]		at ( 0, {\row{8}}) {\fontsize{8pt}{1em}\selectfont{}leg. #4};
	\node[bb] (boundbox) {};

	\draw ({-\lw / 2 - 7mm},{- \lh / 2}) -- ({-\lw / 2 - 2mm)}, {-\lh / 2}) ;
	\draw ({-\lw / 2 - 7mm},{+ \lh / 2}) -- ({-\lw / 2 - 2mm)}, { \lh / 2}) ;
	\draw ({ \lw / 2 + 7mm},{- \lh / 2}) -- ({ \lw / 2 + 2mm)}, {-\lh / 2}) ;
	\draw ({ \lw / 2 + 7mm},{+ \lh / 2}) -- ({ \lw / 2 + 2mm)}, { \lh / 2}) ;

	\draw ({-\lw / 2},{- \lh / 2 - 7mm}) -- ({-\lw / 2)}, {-\lh / 2 - 2mm}) ;
	\draw ({-\lw / 2},{+ \lh / 2 + 7mm}) -- ({-\lw / 2)}, { \lh / 2 + 2mm}) ;
	\draw ({ \lw / 2},{- \lh / 2 - 7mm}) -- ({ \lw / 2)}, {-\lh / 2 - 2mm}) ;
	\draw ({ \lw / 2},{+ \lh / 2 + 7mm}) -- ({ \lw / 2)}, { \lh / 2 + 2mm}) ;

	\node[counter]		at ( {\lw / 2 + 3mm}, { - \lw / 2 + 1mm}) {\the\counter};

	\end{tikzpicture}
	\advance\counter by 1
	\advance\modulo by 1
	\ifnum \modulo = 3
		\\
		\modulo 0
	\else
		%nothing
	\fi
}

\begin{document}

\newcount\counter
\newcount\modulo
\counter 0
\modulo 0

%\sffamily

\input{labels}

\end{document}
